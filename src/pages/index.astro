---
import "../layouts/ComponentsLayout/Layout.module.css";
import Home from "../components/Home/Home.astro";
import Icon from "../components/Icon/Icon.astro";
import { subjectsList } from "./subjects-list.ts";
// export const prerender = true;
---

<Home>
  {subjectsList.map((subject) => <Icon link={subject.link} image={subject.image} alt={subject?.name} />)}
</Home>
